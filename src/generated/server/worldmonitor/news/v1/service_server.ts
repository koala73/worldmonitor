// Code generated by protoc-gen-ts-server. DO NOT EDIT.
// source: worldmonitor/news/v1/service.proto

export interface ListNewsItemsRequest {
  timeRange?: TimeRange;
  pagination?: PaginationRequest;
  source: string;
  alertsOnly: boolean;
}

export interface TimeRange {
  start: number;
  end: number;
}

export interface PaginationRequest {
  pageSize: number;
  cursor: string;
}

export interface ListNewsItemsResponse {
  items: NewsItem[];
  pagination?: PaginationResponse;
}

export interface NewsItem {
  source: string;
  title: string;
  link: string;
  publishedAt: number;
  isAlert: boolean;
  threat?: ThreatClassification;
  location?: GeoCoordinates;
  locationName: string;
}

export interface ThreatClassification {
  level: ThreatLevel;
  category: string;
  confidence: number;
}

export interface GeoCoordinates {
  latitude: number;
  longitude: number;
}

export interface PaginationResponse {
  nextCursor: string;
  totalCount: number;
}

export interface SummarizeHeadlinesRequest {
  maxHeadlines: number;
  topic: string;
}

export interface SummarizeHeadlinesResponse {
  summary?: HeadlineSummary;
}

export interface HeadlineSummary {
  text: string;
  headlineCount: number;
  generatedAt: number;
  model: string;
}

export interface SummarizeArticleRequest {
  provider: string;
  headlines: string[];
  mode: string;
  geoContext: string;
  variant: string;
  lang: string;
}

export interface SummarizeArticleResponse {
  summary: string;
  model: string;
  provider: string;
  cached: boolean;
  tokens: number;
  fallback: boolean;
  skipped: boolean;
  reason: string;
  error: string;
  errorType: string;
}

export type ThreatLevel = "THREAT_LEVEL_UNSPECIFIED" | "THREAT_LEVEL_LOW" | "THREAT_LEVEL_MEDIUM" | "THREAT_LEVEL_HIGH" | "THREAT_LEVEL_CRITICAL";

export interface FieldViolation {
  field: string;
  description: string;
}

export class ValidationError extends Error {
  violations: FieldViolation[];

  constructor(violations: FieldViolation[]) {
    super("Validation failed");
    this.name = "ValidationError";
    this.violations = violations;
  }
}

export class ApiError extends Error {
  statusCode: number;
  body: string;

  constructor(statusCode: number, message: string, body: string) {
    super(message);
    this.name = "ApiError";
    this.statusCode = statusCode;
    this.body = body;
  }
}

export interface ServerContext {
  request: Request;
  pathParams: Record<string, string>;
  headers: Record<string, string>;
}

export interface ServerOptions {
  onError?: (error: unknown, req: Request) => Response | Promise<Response>;
  validateRequest?: (methodName: string, body: unknown) => FieldViolation[] | undefined;
}

export interface RouteDescriptor {
  method: string;
  path: string;
  handler: (req: Request) => Promise<Response>;
}

export interface NewsServiceHandler {
  listNewsItems(ctx: ServerContext, req: ListNewsItemsRequest): Promise<ListNewsItemsResponse>;
  summarizeHeadlines(ctx: ServerContext, req: SummarizeHeadlinesRequest): Promise<SummarizeHeadlinesResponse>;
  summarizeArticle(ctx: ServerContext, req: SummarizeArticleRequest): Promise<SummarizeArticleResponse>;
}

export function createNewsServiceRoutes(
  handler: NewsServiceHandler,
  options?: ServerOptions,
): RouteDescriptor[] {
  return [
    {
      method: "POST",
      path: "/api/news/v1/list-news-items",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const body = await req.json() as ListNewsItemsRequest;
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("listNewsItems", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.listNewsItems(ctx, body);
          return new Response(JSON.stringify(result as ListNewsItemsResponse), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
    {
      method: "POST",
      path: "/api/news/v1/summarize-headlines",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const body = await req.json() as SummarizeHeadlinesRequest;
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("summarizeHeadlines", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.summarizeHeadlines(ctx, body);
          return new Response(JSON.stringify(result as SummarizeHeadlinesResponse), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
    {
      method: "POST",
      path: "/api/news/v1/summarize-article",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const body = await req.json() as SummarizeArticleRequest;
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("summarizeArticle", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.summarizeArticle(ctx, body);
          return new Response(JSON.stringify(result as SummarizeArticleResponse), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
  ];
}

