// Code generated by protoc-gen-ts-client. DO NOT EDIT.
// source: worldmonitor/news/v1/service.proto

export interface ListNewsItemsRequest {
  timeRange?: TimeRange;
  pagination?: PaginationRequest;
  source: string;
  alertsOnly: boolean;
}

export interface TimeRange {
  start: number;
  end: number;
}

export interface PaginationRequest {
  pageSize: number;
  cursor: string;
}

export interface ListNewsItemsResponse {
  items: NewsItem[];
  pagination?: PaginationResponse;
}

export interface NewsItem {
  source: string;
  title: string;
  link: string;
  publishedAt: number;
  isAlert: boolean;
  threat?: ThreatClassification;
  location?: GeoCoordinates;
  locationName: string;
}

export interface ThreatClassification {
  level: ThreatLevel;
  category: string;
  confidence: number;
}

export interface GeoCoordinates {
  latitude: number;
  longitude: number;
}

export interface PaginationResponse {
  nextCursor: string;
  totalCount: number;
}

export interface SummarizeHeadlinesRequest {
  maxHeadlines: number;
  topic: string;
}

export interface SummarizeHeadlinesResponse {
  summary?: HeadlineSummary;
}

export interface HeadlineSummary {
  text: string;
  headlineCount: number;
  generatedAt: number;
  model: string;
}

export interface SummarizeArticleRequest {
  provider: string;
  headlines: string[];
  mode: string;
  geoContext: string;
  variant: string;
  lang: string;
}

export interface SummarizeArticleResponse {
  summary: string;
  model: string;
  provider: string;
  cached: boolean;
  tokens: number;
  fallback: boolean;
  skipped: boolean;
  reason: string;
  error: string;
  errorType: string;
}

export type ThreatLevel = "THREAT_LEVEL_UNSPECIFIED" | "THREAT_LEVEL_LOW" | "THREAT_LEVEL_MEDIUM" | "THREAT_LEVEL_HIGH" | "THREAT_LEVEL_CRITICAL";

export interface FieldViolation {
  field: string;
  description: string;
}

export class ValidationError extends Error {
  violations: FieldViolation[];

  constructor(violations: FieldViolation[]) {
    super("Validation failed");
    this.name = "ValidationError";
    this.violations = violations;
  }
}

export class ApiError extends Error {
  statusCode: number;
  body: string;

  constructor(statusCode: number, message: string, body: string) {
    super(message);
    this.name = "ApiError";
    this.statusCode = statusCode;
    this.body = body;
  }
}

export interface NewsServiceClientOptions {
  fetch?: typeof fetch;
  defaultHeaders?: Record<string, string>;
}

export interface NewsServiceCallOptions {
  headers?: Record<string, string>;
  signal?: AbortSignal;
}

export class NewsServiceClient {
  private baseURL: string;
  private fetchFn: typeof fetch;
  private defaultHeaders: Record<string, string>;

  constructor(baseURL: string, options?: NewsServiceClientOptions) {
    this.baseURL = baseURL.replace(/\/+$/, "");
    this.fetchFn = options?.fetch ?? globalThis.fetch;
    this.defaultHeaders = { ...options?.defaultHeaders };
  }

  async listNewsItems(req: ListNewsItemsRequest, options?: NewsServiceCallOptions): Promise<ListNewsItemsResponse> {
    let path = "/api/news/v1/list-news-items";
    const url = this.baseURL + path;

    const headers: Record<string, string> = {
      "Content-Type": "application/json",
      ...this.defaultHeaders,
      ...options?.headers,
    };

    const resp = await this.fetchFn(url, {
      method: "POST",
      headers,
      body: JSON.stringify(req),
      signal: options?.signal,
    });

    if (!resp.ok) {
      return this.handleError(resp);
    }

    return await resp.json() as ListNewsItemsResponse;
  }

  async summarizeHeadlines(req: SummarizeHeadlinesRequest, options?: NewsServiceCallOptions): Promise<SummarizeHeadlinesResponse> {
    let path = "/api/news/v1/summarize-headlines";
    const url = this.baseURL + path;

    const headers: Record<string, string> = {
      "Content-Type": "application/json",
      ...this.defaultHeaders,
      ...options?.headers,
    };

    const resp = await this.fetchFn(url, {
      method: "POST",
      headers,
      body: JSON.stringify(req),
      signal: options?.signal,
    });

    if (!resp.ok) {
      return this.handleError(resp);
    }

    return await resp.json() as SummarizeHeadlinesResponse;
  }

  async summarizeArticle(req: SummarizeArticleRequest, options?: NewsServiceCallOptions): Promise<SummarizeArticleResponse> {
    let path = "/api/news/v1/summarize-article";
    const url = this.baseURL + path;

    const headers: Record<string, string> = {
      "Content-Type": "application/json",
      ...this.defaultHeaders,
      ...options?.headers,
    };

    const resp = await this.fetchFn(url, {
      method: "POST",
      headers,
      body: JSON.stringify(req),
      signal: options?.signal,
    });

    if (!resp.ok) {
      return this.handleError(resp);
    }

    return await resp.json() as SummarizeArticleResponse;
  }

  private async handleError(resp: Response): Promise<never> {
    const body = await resp.text();
    if (resp.status === 400) {
      try {
        const parsed = JSON.parse(body);
        if (parsed.violations) {
          throw new ValidationError(parsed.violations);
        }
      } catch (e) {
        if (e instanceof ValidationError) throw e;
      }
    }
    throw new ApiError(resp.status, `Request failed with status ${resp.status}`, body);
  }
}

