// Code generated by protoc-gen-ts-client. DO NOT EDIT.
// source: worldmonitor/claude/v1/service.proto

export interface SummarizeRequest {
  headlines: string[];
  region: string;
  language: string;
  variant: string;
}

export interface SummarizeResponse {
  summary: string;
  keyPoints: string[];
  sentiment: string;
  provider: string;
  status: string;
  errorMessage: string;
  inputTokens: number;
  outputTokens: number;
}

export interface AnalyzeRequest {
  query: string;
  contextData: string[];
  region: string;
}

export interface AnalyzeResponse {
  analysis: string;
  keyFindings: string[];
  riskLevel: string;
  status: string;
  errorMessage: string;
  inputTokens: number;
  outputTokens: number;
}

export interface PredictRequest {
  scenario: string;
  evidence: string[];
  timeframe: string;
}

export interface DimensionScore {
  name: string;
  score: number;
  weight: number;
  reasoning: string;
}

export interface PredictResponse {
  dimensions: DimensionScore[];
  overallProbability: number;
  confidence: string;
  timeframe: string;
  narrative: string;
  status: string;
  errorMessage: string;
  inputTokens: number;
  outputTokens: number;
}

export interface FieldViolation {
  field: string;
  description: string;
}

export class ValidationError extends Error {
  violations: FieldViolation[];

  constructor(violations: FieldViolation[]) {
    super("Validation failed");
    this.name = "ValidationError";
    this.violations = violations;
  }
}

export class ApiError extends Error {
  statusCode: number;
  body: string;

  constructor(statusCode: number, message: string, body: string) {
    super(message);
    this.name = "ApiError";
    this.statusCode = statusCode;
    this.body = body;
  }
}

export interface ClaudeServiceClientOptions {
  fetch?: typeof fetch;
  defaultHeaders?: Record<string, string>;
}

export interface ClaudeServiceCallOptions {
  headers?: Record<string, string>;
  signal?: AbortSignal;
}

export class ClaudeServiceClient {
  private baseURL: string;
  private fetchFn: typeof fetch;
  private defaultHeaders: Record<string, string>;

  constructor(baseURL: string, options?: ClaudeServiceClientOptions) {
    this.baseURL = baseURL.replace(/\/+$/, "");
    this.fetchFn = options?.fetch ?? globalThis.fetch;
    this.defaultHeaders = { ...options?.defaultHeaders };
  }

  async summarize(req: SummarizeRequest, options?: ClaudeServiceCallOptions): Promise<SummarizeResponse> {
    const path = "/api/claude/v1/summarize";
    const url = this.baseURL + path;

    const headers: Record<string, string> = {
      "Content-Type": "application/json",
      ...this.defaultHeaders,
      ...options?.headers,
    };

    const resp = await this.fetchFn(url, {
      method: "POST",
      headers,
      body: JSON.stringify(req),
      signal: options?.signal,
    });

    if (!resp.ok) {
      return this.handleError(resp);
    }

    return await resp.json() as SummarizeResponse;
  }

  async analyze(req: AnalyzeRequest, options?: ClaudeServiceCallOptions): Promise<AnalyzeResponse> {
    const path = "/api/claude/v1/analyze";
    const url = this.baseURL + path;

    const headers: Record<string, string> = {
      "Content-Type": "application/json",
      ...this.defaultHeaders,
      ...options?.headers,
    };

    const resp = await this.fetchFn(url, {
      method: "POST",
      headers,
      body: JSON.stringify(req),
      signal: options?.signal,
    });

    if (!resp.ok) {
      return this.handleError(resp);
    }

    return await resp.json() as AnalyzeResponse;
  }

  async predict(req: PredictRequest, options?: ClaudeServiceCallOptions): Promise<PredictResponse> {
    const path = "/api/claude/v1/predict";
    const url = this.baseURL + path;

    const headers: Record<string, string> = {
      "Content-Type": "application/json",
      ...this.defaultHeaders,
      ...options?.headers,
    };

    const resp = await this.fetchFn(url, {
      method: "POST",
      headers,
      body: JSON.stringify(req),
      signal: options?.signal,
    });

    if (!resp.ok) {
      return this.handleError(resp);
    }

    return await resp.json() as PredictResponse;
  }

  private async handleError(resp: Response): Promise<never> {
    const body = await resp.text();
    if (resp.status === 400) {
      try {
        const parsed = JSON.parse(body);
        if (parsed.violations) {
          throw new ValidationError(parsed.violations);
        }
      } catch (e) {
        if (e instanceof ValidationError) throw e;
      }
    }
    throw new ApiError(resp.status, `Request failed with status ${resp.status}`, body);
  }
}
