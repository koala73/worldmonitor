// @ts-nocheck
// Code generated by protoc-gen-ts-client. DO NOT EDIT.
// source: worldmonitor/reits/v1/service.proto

export interface ListReitQuotesRequest {
  sector: string;
  symbols: string[];
  market: string;
}

export interface ListReitQuotesResponse {
  quotes: ReitQuote[];
  regime: ReitRegime;
  aiBriefing: string;
  sectorRotation: SectorRotationSignal[];
  stale: boolean;
  lastUpdated: string;
}

export interface ReitQuote {
  symbol: string;
  name: string;
  sector: string;
  price: number;
  change: number;
  dividendYield: number;
  sparkline: number[];
  disasterExposureScore: number;
  market: string;
}

export interface SectorRotationSignal {
  sector: string;
  signal: string;
  reason: string;
}

export interface GetReitCorrelationRequest {
}

export interface GetReitCorrelationResponse {
  indicators: FredIndicatorSnapshot[];
  correlations: CorrelationCoefficient[];
  regime: ReitRegime;
  sectorRotation: SectorRotationSignal[];
  yieldSpread: number;
  lastUpdated: string;
}

export interface FredIndicatorSnapshot {
  seriesId: string;
  name: string;
  value: number;
  changeDescription: string;
  direction: string;
}

export interface CorrelationCoefficient {
  sector: string;
  indicatorId: string;
  indicatorName: string;
  coefficient: number;
  interpretation: string;
}

export interface ListReitPropertiesRequest {
  sector: string;
  reitSymbol: string;
}

export interface ListReitPropertiesResponse {
  properties: ReitProperty[];
  exposureSummaries: ReitExposureSummary[];
  lastUpdated: string;
}

export interface ReitProperty {
  reitSymbol: string;
  propertyName: string;
  lat: number;
  lng: number;
  sector: string;
  propertyType: string;
  sqft: string;
  city: string;
  state: string;
  metro: string;
}

export interface ReitExposureSummary {
  reitSymbol: string;
  disasterExposureScore: number;
  seismicZoneCount: number;
  wildfireRiskCount: number;
  hurricaneCorridorCount: number;
}

export interface GetReitSocialSentimentRequest {
  reitSymbol: string;
}

export interface GetReitSocialSentimentResponse {
  sentiments: ReitSocial[];
  stale: boolean;
  lastUpdated: string;
  unavailableReason: string;
}

export interface ReitSocial {
  reitSymbol: string;
  socialHealthScore: number;
  avgRating: number;
  reviewVelocity: number;
  positiveKeywords: string[];
  negativeKeywords: string[];
  tenantRiskSignals: string[];
  sector: string;
}

export type ReitRegime = "REIT_REGIME_UNSPECIFIED" | "REIT_REGIME_FAVORABLE" | "REIT_REGIME_CAUTIOUS" | "REIT_REGIME_STRESS" | "REIT_REGIME_NEUTRAL";

export interface FieldViolation {
  field: string;
  description: string;
}

export class ValidationError extends Error {
  violations: FieldViolation[];

  constructor(violations: FieldViolation[]) {
    super("Validation failed");
    this.name = "ValidationError";
    this.violations = violations;
  }
}

export class ApiError extends Error {
  statusCode: number;
  body: string;

  constructor(statusCode: number, message: string, body: string) {
    super(message);
    this.name = "ApiError";
    this.statusCode = statusCode;
    this.body = body;
  }
}

export interface ReitsServiceClientOptions {
  fetch?: typeof fetch;
  defaultHeaders?: Record<string, string>;
}

export interface ReitsServiceCallOptions {
  headers?: Record<string, string>;
  signal?: AbortSignal;
}

export class ReitsServiceClient {
  private baseURL: string;
  private fetchFn: typeof fetch;
  private defaultHeaders: Record<string, string>;

  constructor(baseURL: string, options?: ReitsServiceClientOptions) {
    this.baseURL = baseURL.replace(/\/+$/, "");
    this.fetchFn = options?.fetch ?? globalThis.fetch;
    this.defaultHeaders = { ...options?.defaultHeaders };
  }

  async listReitQuotes(req: ListReitQuotesRequest, options?: ReitsServiceCallOptions): Promise<ListReitQuotesResponse> {
    let path = "/api/reits/v1/list-reit-quotes";
    const params = new URLSearchParams();
    if (req.sector != null && req.sector !== "") params.set("sector", String(req.sector));
    if (req.symbols != null && req.symbols !== "") params.set("symbols", String(req.symbols));
    if (req.market != null && req.market !== "") params.set("market", String(req.market));
    const url = this.baseURL + path + (params.toString() ? "?" + params.toString() : "");

    const headers: Record<string, string> = {
      "Content-Type": "application/json",
      ...this.defaultHeaders,
      ...options?.headers,
    };

    const resp = await this.fetchFn(url, {
      method: "GET",
      headers,
      signal: options?.signal,
    });

    if (!resp.ok) {
      return this.handleError(resp);
    }

    return await resp.json() as ListReitQuotesResponse;
  }

  async getReitCorrelation(req: GetReitCorrelationRequest, options?: ReitsServiceCallOptions): Promise<GetReitCorrelationResponse> {
    let path = "/api/reits/v1/get-reit-correlation";
    const url = this.baseURL + path;

    const headers: Record<string, string> = {
      "Content-Type": "application/json",
      ...this.defaultHeaders,
      ...options?.headers,
    };

    const resp = await this.fetchFn(url, {
      method: "GET",
      headers,
      signal: options?.signal,
    });

    if (!resp.ok) {
      return this.handleError(resp);
    }

    return await resp.json() as GetReitCorrelationResponse;
  }

  async listReitProperties(req: ListReitPropertiesRequest, options?: ReitsServiceCallOptions): Promise<ListReitPropertiesResponse> {
    let path = "/api/reits/v1/list-reit-properties";
    const params = new URLSearchParams();
    if (req.sector != null && req.sector !== "") params.set("sector", String(req.sector));
    if (req.reitSymbol != null && req.reitSymbol !== "") params.set("reit_symbol", String(req.reitSymbol));
    const url = this.baseURL + path + (params.toString() ? "?" + params.toString() : "");

    const headers: Record<string, string> = {
      "Content-Type": "application/json",
      ...this.defaultHeaders,
      ...options?.headers,
    };

    const resp = await this.fetchFn(url, {
      method: "GET",
      headers,
      signal: options?.signal,
    });

    if (!resp.ok) {
      return this.handleError(resp);
    }

    return await resp.json() as ListReitPropertiesResponse;
  }

  async getReitSocialSentiment(req: GetReitSocialSentimentRequest, options?: ReitsServiceCallOptions): Promise<GetReitSocialSentimentResponse> {
    let path = "/api/reits/v1/get-reit-social-sentiment";
    const params = new URLSearchParams();
    if (req.reitSymbol != null && req.reitSymbol !== "") params.set("reit_symbol", String(req.reitSymbol));
    const url = this.baseURL + path + (params.toString() ? "?" + params.toString() : "");

    const headers: Record<string, string> = {
      "Content-Type": "application/json",
      ...this.defaultHeaders,
      ...options?.headers,
    };

    const resp = await this.fetchFn(url, {
      method: "GET",
      headers,
      signal: options?.signal,
    });

    if (!resp.ok) {
      return this.handleError(resp);
    }

    return await resp.json() as GetReitSocialSentimentResponse;
  }

  private async handleError(resp: Response): Promise<never> {
    const body = await resp.text();
    if (resp.status === 400) {
      try {
        const parsed = JSON.parse(body);
        if (parsed.violations) {
          throw new ValidationError(parsed.violations);
        }
      } catch (e) {
        if (e instanceof ValidationError) throw e;
      }
    }
    throw new ApiError(resp.status, `Request failed with status ${resp.status}`, body);
  }
}

