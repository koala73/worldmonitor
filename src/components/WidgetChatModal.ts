// Stub: WidgetChatModal removed in REITs-only variant

export function openWidgetChatModal(_options: {
  mode?: string;
  existingSpec?: unknown;
  onComplete?: (updated: { id: string }) => void;
}): void {
  // no-op in REITs-only mode
}
