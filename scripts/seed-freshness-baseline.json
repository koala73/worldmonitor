{
  "$comment": [
    "Accepted-problem baseline for scripts/check-seed-freshness.mjs.",
    "",
    "The acceptance gate fails on any compact-health problem that is NOT listed here.",
    "An entry means: this degradation is known, has an owner record, and must not page.",
    "It does NOT mean the problem is acceptable forever -- expiresAt forces a re-review,",
    "and the gate fails outright once that date passes.",
    "",
    "A temporary rollout entry may add an entry-level `expiresAt` bounded to the first",
    "scheduled cron window. At or after that instant, its live problem blocks even when",
    "the root expiresAt is later. A health-probe cutover requires either a completed",
    "Railway-side pre-seed or this owner-bound, expiring acknowledgement.",
    "",
    "Entries are matched on name:status exactly. A source that recovers is reported as",
    "'recovered' on the next run (prune it); a source that fails with a DIFFERENT status",
    "than the one baselined is treated as new and blocks, which is the point.",
    "",
    "Every entry carries a real tracking issue, never the PR that introduced the entry:",
    "a merged PR closes, and a suppression pointing at a closed PR has no owner.",
    "",
    "Softening fault statuses is the marketImplications blind spot the ON_DEMAND_KEYS policy",
    "block in api/health.js exists to prevent."
  ],
  "expiresAt": "2026-08-27",
  "acknowledged": [
    {
      "name": "mineralProduction",
      "status": "EMPTY",
      "issue": 6439,
      "reason": "New USGS/BGS mineral-production probe. seed-bundle-static-ref runs daily at 03:00 UTC (`0 3 * * *` in scripts/railway-services.json), so the key cannot exist until the first tick after this PR deploys; EMPTY is expected until then. Anchored to that real cron window: MAX_ROLLOUT_WINDOW_MS caps firstScheduledRunAt-activatedAt at 24h, and a daily tick fits inside it. If this PR has not merged by 2026-08-14T03:00Z, re-anchor both to the next 03:00 UTC tick.",
      "expiresAt": "2026-08-14T03:00:00.000Z",
      "cutover": {
        "probeKey": "seed-meta:supply-chain:mineral-production",
        "activatedAt": "2026-08-13T06:00:00.000Z",
        "firstScheduledRunAt": "2026-08-14T03:00:00.000Z"
      }
    },
    {
      "name": "ttcAlerts",
      "status": "EMPTY",
      "issue": 6623,
      "expiresAt": "2026-08-15T12:00:00.000Z",
      "reason": "New TTC GTFS-RT service-alerts probe. seed-ttc-alerts cron is */5 * * * *; EMPTY is expected until the first Railway tick after this branch deploys. Expiry equals firstScheduledRunAt so a missed first publication blocks acceptance rather than staying silent. Re-anchored 2026-08-14T18:00Z because the previous window (expiry 2026-08-14T12:00Z) lapsed before merge. The PR-time cutover check only validates that a cutover EXISTS — it never compares expiresAt to the clock — so an expired anchor ships green and reds the scheduled monitor minutes after merge. The */5 cron is not the binding constraint: seed-ttc-alerts is one of six services not yet created on Railway, so this window covers provisioning, not just deploy. If it has not merged AND been provisioned by 2026-08-15T12:00Z, re-anchor both again.",
      "cutover": {
        "probeKey": "seed-meta:transit:ttc-alerts",
        "activatedAt": "2026-08-14T18:00:00.000Z",
        "firstScheduledRunAt": "2026-08-15T12:00:00.000Z"
      }
    }
  ]
}
