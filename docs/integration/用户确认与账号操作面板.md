# 用户确认与账号操作面板

**产品：** 全球实时热点追踪·探长版
**当前阶段：** Phase 0 已封口；现在只处理 Phase 1 的法律与独立品牌确认。
**本文件路径：** `D:\使用AI专属文件夹\global-intelligence-earth\worldmonitor-integrated\docs\integration\用户确认与账号操作面板.md`

## 现在执行：法律与品牌确认

请仅把下面两项的 `确认值：未确认` 改成 `确认值：我确认`。不要在本文件、聊天、截图、Git 历史或前端变量中填写任何 API 密钥。

- [ ] 我确认采用 WorldMonitor 衍生母体，并接受适用于该网络服务和分发方式的 AGPL-3.0-only 义务：保留许可证与版权声明；如果向网络用户提供运行本修改版本的服务，则向这些用户提供对应完整源代码；不把组合工程作为私有闭源网络服务发布。
  - 确认值：未确认

- [ ] 我确认使用自己的品牌“全球实时热点追踪·探长版”，不冒充 WorldMonitor 官方产品，不把其 Logo 当作本产品主 Logo；产品关于页将注明 `Based on World Monitor, modified and distributed under AGPL-3.0-only.` 并链接原仓库。
  - 确认值：未确认

- [ ] 我理解：若复制或实质性改编 PokieTicker 内容，必须保留其 MIT 版权和许可证文本；这不会把历史 SQLite 数据伪装为实时行情。
  - 确认值：未确认

## 密钥与文件安全红线

1. 密钥只能由你本人写入本机 `D:\使用AI专属文件夹\global-intelligence-earth\worldmonitor-integrated\.env.local` 或你拥有的部署平台 Secret 设置。
2. 本文件只记录 `未配置`、`已配置（未验证）` 或 `已验证`，绝不记录密钥正文、尾号、截图或复制文本。
3. 不要把密钥发送到聊天，也不要加入 Git、提交说明、日志、浏览器地址栏、截图或 `VITE_*` 前端变量。
4. 填写后由 Codex 执行不回显密钥的健康检查；口头说“已配置”不能替代验证结果。
5. 没有 Provider 密钥时，适配器、禁用态、契约测试和其他不依赖密钥的工作会继续；界面必须如实显示 `NOT_CONFIGURED`、延迟或历史状态。

## Provider 与账户操作矩阵

| 能力 / Provider | 必须性与当前状态 | 官方申请或控制台 | 应选择的授权等级 | Secret / 填写位置 | 安全验证命令或状态 |
|---|---|---|---|---|---|
| 美股真实分钟/逐笔行情 — Massive 或等价持牌源 | 达成“真实实时 K 线”时必须；目前 `未配置`。只做历史研究时可暂不申请。 | [Massive 官方](https://massive.com/) | 明确覆盖目标展示方式、实时或延迟级别、并发、持久化和再分发权；免费试用或无再分发授权不能标为实时。 | `MASSIVE_API_KEY`（**Phase 2 适配器尚未创建此变量**）；届时只写入 `.env.local` 或部署 Secret。 | 当前没有真实行情适配器，不能假装“已验证”。Phase 2 将添加不回显密钥的 Provider health / contract test。 |
| 全球 AIS 船位 — AISStream | 需要全球实时/近实时 AIS 时必须；不阻塞无密钥禁用态实现；目前 `未配置`。 | [AISStream 官方](https://aisstream.io/) | 接受其服务条款，确认 WebSocket 使用范围、频率和展示许可。 | `AISSTREAM_API_KEY`；`.env.local` 或部署 Secret。绝不使用 `VITE_AISSTREAM_API_KEY`。 | 配置后由 Codex 验证真实 MMSI、packet time、`fetchedAt`、freshness 与断线降级；AIS 目的地仍只标注“自报字段”。 |
| 股票降级报价 — Finnhub | 可选的降级链；目前 `未配置`。 | [Finnhub 注册](https://finnhub.io/register) | 选择符合个人/商业展示和所需延迟等级的计划；免费层不自动等于可再分发实时。 | `FINNHUB_API_KEY`；`.env.local` 或部署 Secret。 | `npm run security:vite-env-secrets -- --strict-local` 只检查前端不泄密；Phase 2 才会加入请求/延迟契约验证。 |
| 股票降级报价 — Alpha Vantage | 可选的降级链；目前 `未配置`。 | [Alpha Vantage 申请](https://www.alphavantage.co/support/#api-key) | 按其数据产品、调用频率和展示许可选计划；不得把日终/限频 API 标为实时。 | `ALPHA_VANTAGE_API_KEY`；`.env.local` 或部署 Secret。 | `npm run security:vite-env-secrets -- --strict-local`；Provider 实际新鲜度和许可将在适配器完成后测试。 |
| 新闻深度分析 — Anthropic 或项目支持的 AI Provider | 可选；不会阻塞真实行情或基础新闻显示；目前 `未配置`。 | [Anthropic Console](https://console.anthropic.com/) | 由你本人接受服务条款并选择满足预算、数据处理和商业使用要求的计划。 | 当前原生变量 `ANTHROPIC_API_KEY`；`.env.local` 或部署 Secret。 | 配置后只验证服务健康与模型元数据；输出必须保留不确定性，不能把情绪或相关性写成确定因果。 |
| 缓存、任务与健康 — Upstash Redis / 自托管 Redis | 本地单机开发可选；生产缓存、队列和监控建议配置；目前 `未配置`。 | [Upstash Console](https://upstash.com/) | 选择满足环境、数据保留和流量要求的数据库计划；不要在未确认所有权时创建付费资源。 | `UPSTASH_REDIS_REST_URL` 与 `UPSTASH_REDIS_REST_TOKEN`，或自托管 `REDIS_PASSWORD` / `REDIS_TOKEN`；只写 `.env.local` / Secret。 | `npm run security:vite-env-secrets -- --strict-local`；配置后由 Codex 运行 Redis 连接、权限与缓存健康检查。 |
| 国家/产品贸易 — UN Comtrade | 中国工厂贸易流的推荐来源；无 key 时该功能保持禁用或只用合法快照；目前 `未配置`。 | [UN Comtrade Developer](https://comtradedeveloper.un.org/) | 选择覆盖请求量、商用展示和数据缓存需求的 access level。 | `COMTRADE_API_KEYS`；`.env.local` 或部署 Secret。 | Phase 7 将添加导入与数据谱系验证；目前没有可声称通过的 live health command。 |
| 中国海关月度统计 | 官方区域贸易事实所需；需要你本人合法查询、导出或按官方流程申请；目前 `未配置`。 | [中国海关统计数据查询平台](https://stats.customs.gov.cn/) | 只接受官方允许的访问、导出和使用方式；不要爬取受限页面或绕过频率限制。 | 无通用 Secret。等 Phase 7 审核导入器创建后，按其指定目录导入经许可 CSV；不要现在复制数据文件。 | 当前无导入器，不能验证，也不能把地方宣传材料写成海关出口事实。 |
| 逐票/逐船货物 — 商业提单 Provider | 若要声称船名、箱号、收发货人或逐票事实则必须；否则可选且必须显示没有逐船货物证据。 | 由你选择并签约的合法 Provider，例如 [S&P Global Panjiva](https://www.spglobal.com/market-intelligence/en/solutions/products/panjiva-supply-chain-intelligence) 或 [PIERS](https://www.spglobal.com/market-intelligence/en/solutions/products/piers)。 | 合同须覆盖目标国家/地区、字段、展示、保留和再分发；免费 AIS 不能替代。 | 变量名和导入位置待 Phase 7 按实际合同 Provider 固化，避免创建同义变量。 | Phase 7 将验证记录、许可和 `BILL_OF_LADING_OBSERVED` 谱系；未签约前页面不得出现具体货物链路事实。 |
| 部署平台 — Vercel / Railway / 域名 | 本地开发可选；生产部署必须由你确认平台与域名所有权后再做；目前 `未配置`。 | [Vercel](https://vercel.com/) / [Railway](https://railway.app/) | 由你本人选择可承担的计划、区域与所有权；不要让 Codex 自动创建付费资源。 | 仅使用平台 Secret；不在仓库或前端配置任何生产密钥。 | 仅在你确认所有权、登录和 Secrets 后，执行对应平台预检；部署前必须通过 `npm run build:full` 与秘密扫描。 |

## 你完成后 Codex 会执行的安全检查

这些命令不读取或打印密钥正文；它们只在对应功能和适配器已经存在时验证配置是否可安全使用。

```powershell
Set-Location 'D:\使用AI专属文件夹\global-intelligence-earth\worldmonitor-integrated'
npm run security:vite-env-secrets -- --strict-local
npm run typecheck:all
```

Provider 实际健康检查、许可/延迟验证、不同股票 OHLC 独立性、AIS 新鲜度和中国贸易导入验证必须等对应适配器在后续 Phase 实现后才会添加。它们现在不能被面板、文档或 mock 数据替代。

## 当前唯一需要你做的动作

不要填写任何密钥。仅将上方前两项的 `确认值：未确认` 改为 `确认值：我确认`，保存本文件；随后回到聊天确认已完成。Codex 会直接读取此文件并继续，不会要求你重新寻找它。
