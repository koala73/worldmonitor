apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: hlidskjalf-dashboard
  namespace: hlidskjalf-monitor
spec:
  host: hlidskjalf.apps.sno.greysson.com
  to:
    kind: Service
    name: hlidskjalf-server-svc
    weight: 100
  port:
    targetPort: 3000
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
